#include <stdio.h>

// Task: check if 2 numbers are a friendly pair
// - I recommend using a helper function
//   to calculate the abundancy index of a number
// - the main function should return your result

// after running the code the terminal prints something like:
// "exited with code=0" -> which shows the return val of main

// in C there is no "True" or "False"
// instead 0 (=false) and 1 (=true) are used
int main() {
    int a = 6;
    int b = 28;
    // TODO: implement me
}
